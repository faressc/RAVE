# Add ons to RAVE readme.md

## Protobuf for AudioExample class

Generate the python code for the AudioExample class using the following command:

```bash
protoc --python_out=. ./proto/audio_example.proto
```
